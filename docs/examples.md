
# Examples

The `examples/` directory contains small usage references for `LibCommand`.

Use examples as starting points, not as copy-paste production code. Always adjust namespaces, plugin lifecycle methods and dependency checks to your own plugin.
